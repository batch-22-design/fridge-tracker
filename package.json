{
  "name": "fridge-tracker",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "db:up": "/Applications/Docker.app/Contents/Resources/bin/docker compose up -d",
    "db:down": "/Applications/Docker.app/Contents/Resources/bin/docker compose down",
    "db:migrate": "npm run migrate -w server",
    "db:reset": "docker compose down -v && docker compose up -d && sleep 2 && npm run migrate -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
